name = "calm_heading_hold"
duration_s = 120.0
dt = 0.05
target_heading = 0.0  # hold north (0 rad)

[wind]
speed = 5.0         # 5 m/s (~10 knots), gentle breeze
direction = 1.047   # ~60° from north (close reach from starboard)

[initial_state]
x = 0.0
y = 0.0
psi = 0.1   # start slightly off target (~6° east of north)
u = 2.0     # initial forward speed 2 m/s (~4 knots)

[quality_gates]
max_heading_deviation_deg = 10.0
max_rudder_rate_deg_per_s = 40.0
max_settling_time_s = 30.0
max_mean_heading_error_deg = 3.0
